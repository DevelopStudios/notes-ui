// src/environments/environment.ts
export const environment = {
  production: false,
  // Ensure this URL matches your Django Codespace address (port 8000)
  // When running locally, Codespaces automatically forwards this port.
  // The trailing slash is important!
  apiUrl: 'http://localhost:8000/api/',
};